package trees;

import javax.swing.tree.TreeNode;

/**
 * Serialization is converting a data structure or object into a sequence of bits so that it can be stored
 * in a file or memory buffer,
 * or transmitted across a network connection link to be reconstructed later in the same or another computer environment.
 *
 * Design an algorithm to serialize and deserialize a binary search tree. There is no restriction on how your
 * serialization/deserialization
 * algorithm should work. You need to ensure that a binary search tree can be serialized to a string, and
 * this string can be deserialized to
 * the original tree structure.
 *
 * The encoded string should be as compact as possible.
 * Input: root = [2,1,3]
 * Output: [2,1,3]
 */
public class SerializeDeSerializeBSTDemo {
    public static void main(String[] args) {
//        Codec ser = new Codec();
//        Codec deser = new Codec();
//        String tree = ser.serialize(root);
//        MyTreeNode ans = deser.deserialize(tree);
//        System.out.println("deserialized String"+ ans.val);
    }
}
//class Codec {
//
//    // Encodes a tree to a single string.
//    public String serialize(MyTreeNode root) {
//
//    }
//
//    // Decodes your encoded data to tree.
//    public MyTreeNode deserialize(String data) {
//
//    }
//}
//class MyTreeNode {
//    int val;
//    MyTreeNode left;
//    MyTreeNode right;
//    MyTreeNode(int x) { val = x; }
//}